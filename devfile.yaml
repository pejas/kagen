schemaVersion: 2.2.0
metadata:
  name: kagen-workspace
  version: 1.0.0
components:
  - name: workspace
    container:
      image: vxcontrol/codebase:latest
      command: ["/bin/sh", "-lc"]
      args:
        - |
          exec tail -f /dev/null
